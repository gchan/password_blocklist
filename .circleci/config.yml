version: 2.1
orbs:
  ruby: circleci/ruby@1.1.4
  
jobs:
  example-job:
    docker:
      # replace with your preferred image
      - image: cimg/base:stable
    steps:
      - ruby/install:
          version: '2.7.3' 
      - ruby/install-deps
      - ruby/rspec-test

workflows:
  example-workflow:
    jobs:
      - example-job